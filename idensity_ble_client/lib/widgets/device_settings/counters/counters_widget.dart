import 'package:flutter/material.dart';

class CountersWidget extends StatelessWidget {
  const CountersWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text("Счетчики"),);
  }
}