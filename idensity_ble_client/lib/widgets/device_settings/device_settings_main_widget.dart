import 'package:flutter/material.dart';

class DeviceSettingsMainWidget extends StatelessWidget {
  const DeviceSettingsMainWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text("Настройки прибора"));
  }
}
