import 'package:flutter/material.dart';

class MeasProcessSettingsWidget extends StatelessWidget {
  const MeasProcessSettingsWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text("Измерительные процессы"),);
  }
}